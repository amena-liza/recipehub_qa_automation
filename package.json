{
  "name": "myblapp",
  "version": "1.0.0",
  "description": "mybl app automation",
  "main": "index.js",
  "scripts": {
    "browserstack-development": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=development npx wdio config/browserStack.wdio.conf.js --suite $SUITE",
    "browserstack-production": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=production npx wdio config/browserStack.wdio.conf.js --suite $SUITE",
    "browserstack-stage": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=stage npx wdio config/browserStack.wdio.conf.js --suite $SUITE",
    "browserstack-test": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=test npx wdio config/browserStack.wdio.conf.js --suite $SUITE",
    "development": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=development npx wdio config/development.wdio.conf.js --suite $SUITE",
    "production": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=production npx wdio config/development.wdio.conf.js --suite $SUITE",
    "stage": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=stage npx wdio config/development.wdio.conf.js --suite $SUITE",
    "test": "rm -rf allure-results && rm -rf allure-report && NODE_ENV=test npx wdio config/development.wdio.conf.js --suite $SUITE",
    "get-report": "allure generate --single-file --clean allure-results && allure open"
  },
  "keywords": [
    "webdriverio",
    "appium",
    "mocha",
    "chai",
    "allure-reporter"
  ],
  "author": "BrainStation23",
  "license": "ISC",
  "dependencies": {
    "@wdio/cli": "^9.8.0",
    "allure-commandline": "^2.32.2",
    "appium": "^2.15.0",
    "appium-doctor": "^1.16.2",
    "axios": "^1.7.9",
    "chai": "^5.1.2",
    "chai-as-promised": "^8.0.1",
    "dotenv": "^16.4.7",
    "dotenv-flow": "^4.1.0",
    "moment-timezone": "^0.5.47",
    "node-xlsx": "^0.24.0"
  },
  "devDependencies": {
    "@wdio/allure-reporter": "^9.6.3",
    "@wdio/appium-service": "^9.8.0",
    "@wdio/browserstack-service": "^9.12.7",
    "@wdio/local-runner": "^9.8.0",
    "@wdio/mocha-framework": "^9.7.3",
    "@wdio/spec-reporter": "^9.8.0",
    "appium-uiautomator2-driver": "^4.2.9",
    "appium-xcuitest-driver": "^8.3.1",
    "ts-node": "^10.9.2"
  }
}
